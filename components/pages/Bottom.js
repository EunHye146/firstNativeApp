import React from 'react'
import ContentList from '../common/ContentList';


function Bottom({ navigation }) {
    return (
        <ContentList type="bottom" navigation={navigation}/>
    );
  }
  
  export default Bottom;